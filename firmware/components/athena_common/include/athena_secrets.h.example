/* athena_secrets.h.example - copy to athena_secrets.h (same directory, gitignored)
 * and fill in the network every Athena board joins. One file serves all boards.
 * Never commit the copy: .gitignore lists athena_secrets.h, keep it that way.
 */
#pragma once

#define ATHENA_WIFI_SSID    "your-network"
#define ATHENA_WIFI_PASS    "your-password"     /* "" for an open network */
